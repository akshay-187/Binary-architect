# Security Rules

- No hardcoded secrets — use `.env` with `python-dotenv`.
- Never log PII or tokens.
- No modifying deployment configs without approval.
- No installing packages without confirmation.
- No connections to unknown hosts.
